# This is a test pipeline of reading events from
# docker files and sending them to stdout

input {
 file {
  path => '/var/log/containers/*.log'
  start_position => 'beginning'
 }
}

output {
  stdout {
    id => 'pipeline_docker_to_stdout'
  }
}
